import { Router } from "express";

/**
 * Legacy GitHub route placeholder. The live app serves posts through tRPC
 * in server/routers.ts; this export remains for compatibility with older imports.
 */
export const router = Router();
export default router;
