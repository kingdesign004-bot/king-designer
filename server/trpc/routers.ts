/**
 * Compatibility entrypoint kept for older GitHub routes.
 * The active application router lives in server/routers.ts.
 */
export { appRouter as router } from "../routers";
export { appRouter } from "../routers";
export type { AppRouter } from "../routers";
