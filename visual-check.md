# Visual verification — 2026-09-02

تمت معاينة `/messages` و`/profile/1` على سطح مكتب 1280×720 بعد تحديث النموذج. صفحة الرسائل تعرض حالة عدم وجود محادثة محددة بشكل واضح، وصفحة الملف الشخصي تعرض ID 10000، زر نسخ ID، زر مشاركة الملف، حالة الاتصال، الدولة، ومعرض الأعمال. الهوية العربية RTL والألوان الأساسية ظاهرة دون أخطاء عرض في هذه المسارات.

## التحقق بعد الاستيراد — 2026-09-02

تمت معاينة المسارات `/` و`/profile/1` و`/messages` و`/notifications` و`/search` و`/admin` على سطح مكتب 1280×720، مع معاينة `/` و`/messages` على هاتف 390×844. الواجهة العربية RTL متماسكة، الرأس ثابت ويعرض الشعار الرمزي المتاح، والصفحة الرئيسية تنتقل من التحميل إلى الحالة الفارغة عند عدم وجود منشورات. تم تهيئة الجداول المطلوبة في قاعدة البيانات، وأصبحت طلبات `feed.list` و`splash.active` و`auth.me` تعيد استجابات ناجحة في المعاينة.

| المسار | النتيجة | الملاحظات |
| --- | --- | --- |
| `/` | سليم | التغذية الفارغة واضحة، الحالة الزخرفية لا تُقدَّم كمنشورات مستخدمين، وتظهر عناصر المجتمع والحساب. |
| `/profile/1` | سليم | بطاقة الملف ومعرّف المستخدم 10000 وأزرار المشاركة والنسخ ومعرض الأعمال ظاهرة. |
| `/messages` | سليم | حالة عدم وجود محادثة محددة واضحة مع زر اكتشاف الأعمال. |
| `/notifications` | سليم | الحالة الفارغة ورسالة التحديث الدوري ظاهرتان دون تشوه بصري. |
| `/search` | سليم | شاشة البحث تعرض الإرشاد عند عدم وجود استعلام أو نتائج. |
| `/admin` | سليم | لوحة الإدارة تعرض الوحدات والإحصاءات الحالية، مع البيانات الفارغة المتوقعة حيث لا توجد سجلات إضافية. |

لا توجد عناصر معلقة تحتاج قرارًا من صاحب المشروع ضمن نطاق الاستيراد الحالي. أما ميزات refund الحقيقية، وضوابط الرصيد والمكافآت المتقدمة، وأي توسيع إضافي للوحدات الإدارية، فتظل أعمالًا مستقلة من قائمة الجودة القديمة وتتطلب قرارًا وظيفيًا أو تكامل دفع قبل تنفيذها، ولم يتم الادعاء بتنفيذها هنا.

## المراجعة النهائية بعد التحسينات العاجلة — 2026-09-02

تمت إعادة معاينة `/notifications` و`/messages` و`/profile/1` و`/` على سطح مكتب 1280×720 بعد آخر تعديل. لم تظهر أخطاء TypeScript أو أخطاء طلبات 5xx في سجلات المعاينة.

| المسار | النتيجة النهائية |
| --- | --- |
| `/notifications` | يظهر زر **تفعيل إشعارات الموقع**، وتبقى الحالة الفارغة واضحة عند عدم وجود تنبيهات. الإذن يُطلب فقط بعد ضغط المستخدم، والتنبيه المحلي يظهر للإشعار الجديد غير المقروء عند السماح. |
| `/messages` | تظهر حالة عدم وجود محادثة عند غياب المعرّف. في المحادثة الفعلية تُحدّث الرسائل عبر polling، وتبدأ الرسالة بحالة الإرسال، ثم تُسجّل كتم الوصول عند جلب المستلم لها، ثم كمقروءة عند فتح المحادثة. |
| `/profile/1` | يظهر المستوى الرقمي، وتوجد معاينة للصورة الشخصية والغلاف قبل الحفظ، مع رفض مبكر للامتدادات غير المدعومة أو الصور الأكبر من 5MB. |
| `/` | الواجهة الرئيسية تعمل مع شاشة البداية الحالية، ولا تتأثر بإضافات الإشعارات أو الرسائل أو المستوى. |

تم اعتماد قاعدة واضحة للمستوى: يبدأ الحساب من 1، ويرتفع مستوى واحدًا لكل خمس منشورات عامة محفوظة فعليًا. الحساب يتم على الخادم، والمستوى للعرض فقط ولا يمكن للمستخدم تعديله يدويًا.

## مراجعة الهوية والرسائل — 2026-09-03

تمت معاينة `/messages` و`/profile/1` و`/` على سطح المكتب والهاتف بعد التعديل. صفحة `/messages` تعرض صندوق المحادثات الفعلي مع محادثة Ahmed Abdelrhman ومعرّف الطرف الآخر، وتظل قابلة للاختيار على الهاتف. صفحة `/profile/1` بدأت بالتحميل في لقطة الهاتف بسبب زمن استعلام الملف، دون ظهور خطأ API في السجلات؛ خادم التطوير ظل يعمل وTypeScript بلا أخطاء. بطاقة `PostCard` تعرض العنوان والوصف والوسوم فوق الصور أو الفيديوهات، بينما تظل الأعمال النصية تعرض محتواها داخل الخلفية الورقية.

## استكمال التنقل والهوية — 2026-09-04

تمت معاينة الصفحة الرئيسية وصفحة الرسائل والملف على سطح المكتب بعد إضافة البحث المتجاوب. البحث ظاهر في رأس الصفحة على اللابتوب، وصندوق الرسائل يحافظ على تخطيطه المركزي. لم تظهر أخطاء TypeScript أو أخطاء API جديدة في سجل الخادم. لقطات المعاينة العامة قد تبدأ بمؤشر تحميل قصير أثناء تهيئة الجلسة والاستعلامات، لذلك يجب اختبار المحادثة والملف بحسابين فعليين بعد تسجيل الدخول.

## تحقق هاتفي بعد مزامنة GitHub — 2026-09-05

تم اختبار `/` و`/messages` و`/profile/1` بعرض 390×844. ظهر شريط البحث على الهاتف بعرض مناسب، وبقيت قائمة المحادثات قابلة للقراءة واللمس، وظهر الملف الشخصي باسم المستخدم والـID وشارة المستوى دون خروج العناصر من الإطار. ظهر في الصفحة الرئيسية غلاف تحميل قصير أثناء الجلسة العامة، وهو متوقع عند عدم وجود session cookie في المعاينة العامة.

## مصدر شارات الاشتراك

تعرض `IdentityBadges` شارة `VIP` أو `PRO` فقط عندما يعيد الخادم خطة اشتراك نشطة من جدول `subscriptions` بحالة `active`، بينما تبقى الخطة `free` بلا شارة. لا يتم اشتقاق الشارة من الدور الإداري أو من رقم المستوى أو من بيانات الواجهة.
